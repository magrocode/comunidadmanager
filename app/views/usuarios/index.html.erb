<% provide(:titulo, 'Usuarios') %>

<div class="row-fluid">
	<div class="side-left">
		<%= render 'comunidads/menu_comunidad' %>
	</div>
	<div class="content-layout">
		<div class="content-wrapper">
			<div class="profile-header">
				<!-- informacion del perfil -->
				<%= render 'usuarios_header' %>
			</div>
			<div class="content">
				<div class="row-fluid">
					<!-- columna de contenidos -->
					<div class="span12">
						<%= render 'usuarios' %>
					</div>
				</div>		
			</div>
		</div>
	</div>
</div>

<script type="text/javascript">
      $(function () {
        /*$('#link_genial').click(function () {
          alert('Destinado al exito!');
          return false;
        })*/

        $('.checkeable').click(function () {
        	var formulario = "#form_admin_" + this.getAttribute('id').split("_")[1];
        	$(formulario).submit();        	
        	//alert('Un peldaño mas ' + formulario);     
        	return true;
        })

        $('.form_in_table').submit(function() {
		  
		    $.ajax({
		    type: this.method,
		    url: this.action,
		    data: $(this).serialize(),
		    success: function(){
		      //alert('Directo al exito!');
		    }
		    });
		  return false;
		});
      });
</script>