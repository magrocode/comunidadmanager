<% provide(:titulo, @usuario.nombre) %>
<div class="row-fluid">
	<div class="side-left">
		<%= render 'menu_usuario' %>
	</div>
	<div class="content-layout">
		<div class="content-wrapper">
			<div class="profile-header">
				<!-- informacion del perfil -->
				<%= render 'permisos_header' %>
			</div>
			<div class="content">
				<div class="row-fluid">
					<!-- columna de contenidos -->
					<div class="span12">
						<%= render 'permisos' %>
					</div>
				</div>
			</div>
		</div>
	</div>
</div>