<div class="row-fluid profile-header">	
	<div class="span10">
		<h2>Todos los usuarios</h2>
	</div>	
	<div class="span2">
		<% if current_user.administrador? %>
			<%= link_to(new_comunidad_usuario_path(@comunidad), class: 'btn btn-primary') do %>
  				<i class="icon-plus-sign icon-white"></i> <strong>Nuevo usuario</strong>
			<% end %>
		<% end %>
	</div>
</div>