<%= form_for(@usuario.relacion_usuario_unidads.build(unidad_id: unidad.id)) do |f| %>
  <div>
  	<%= f.hidden_field :usuario_id %>
  	<%= f.hidden_field :unidad_id %>
  </div>
  <%= f.submit "Autorizar", class: "btn btn-small btn-primary" %>
<% end %>