<%= form_for @usuario.relacion_usuario_unidads.build(unidad_id: unidad.id), html: { class: "form_in_table"} do |f| %>
  <div>
  	<%= f.hidden_field :usuario_id %>
  	<%= f.hidden_field :unidad_id %>
  </div>
  <%= f.submit "Autorizar", class: "btn btn-mini btn-primary" %>
<% end %>