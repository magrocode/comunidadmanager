<%= form_for @unidad.relacion_unidads.find_by_vinculada_id(unidad),
            html: { method: :delete, class: "form_in_table" }, remote: true do |f| %>
  <%= f.submit "Desvincular", class: "btn btn-small" %>
<% end %>