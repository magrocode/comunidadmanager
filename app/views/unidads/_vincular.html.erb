<%= form_for(@unidad.relacion_unidads.build(vinculada_id: unidad.id)) do |f| %>
  <div>
  	<%= f.hidden_field :principal_id %>
  	<%= f.hidden_field :vinculada_id %>
  </div>
  <%= f.submit "Vincular", class: "btn btn-small btn-primary" %>
<% end %>