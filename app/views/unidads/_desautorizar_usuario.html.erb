<%= form_for @unidad.relacion_usuario_unidads.find_by_usuario_id(usuario),
            html: { method: :delete }, remote: true do |f| %>
  <%= f.submit "Desautorizar", class: "btn btn-small" %>
<% end %>