<div class="row-fluid profile-header">	
	<div class="span10">
		<h2>Todas las unidades</h2>
	</div>	
	<div class="span2">
		<% if current_user.administrador? %>
			<%= link_to(new_comunidad_unidad_path(@comunidad), class: 'btn btn-primary') do %>
  				<i class="icon-plus-sign icon-white"></i> <strong>Nueva unidad</strong>
			<% end %>
		<% end %>
	</div>
</div>