<div class="profile-content">	
	<div class="row-fluid">
		<div class="span8 profile-simple">
			<h2>Todos los tipos de unidad</h2>
		</div>
		<div class="span4 profile-actions">
			<% if current_user.administrador?(current_comunidad) or current_user.system_admin? %>
				<%= link_to(new_comunidad_tipounidad_path(@comunidad), class: 'btn btn-small btn-primary') do %>
	  				<i class="icon-plus-sign icon-white"></i> Nuevo tipo
				<% end %>
			<% end %>
		</div>
		<div class="clear"></div>
	</div>	
</div>

