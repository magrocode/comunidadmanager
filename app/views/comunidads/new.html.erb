<% provide(:titulo, 'Sign up')%>
<div class="page-header center"><h1>Sign up</h1></div>

<div class="row">
	<div class="span6 offset3">
		<%= form_for(@comunidad) do |f| %>
			<div class="alert alert-info">
				<a class="close" data-dismiss="alert">×</a>
				<h4 class="alert-heading">Empecemos!</h4><br />
				Crear tu comunidad en bloombee.com es muy facil, primero debes ingresar
				el nombre de la comunidad, edificio o condominio 
				y la informacion del administrador responsable de la cuenta...
			</div>
		
			<%= render 'shared/error_messages', object: f.object %>

			<%= f.label :nombre, "Nombre de Comunidad" %><span class="muted"> (ej: Condominio Nuevo Universo)</span>
			<%= f.text_field :nombre %>
			
			<%= label_tag(:email_administrador, "Email de administrador") %><span class="muted"> (ej: admin@nuevouniverso.com)</span>
  			<%= text_field(:usuario, :email) %>			

  			<%= label_tag(:nombre_administrador, "Nombre de administrador") %><span class="muted"> (ej: Pedro Dozen)</span>
  			<%= text_field(:usuario, :nombre) %>
		
  			<%= label_tag(:password, "Password del administrador") %>
  			<%= password_field(:usuario, :password) %>
  			
  			<%= label_tag(:password_confirmation, "Confirmacion del password") %>
  			<%= password_field(:usuario, :password_confirmation) %>

			<%= f.submit "Crear Comunidad", :class => "btn btn-large btn-primary" %>
		<% end %>
	</div>
</div>
